namespace Domain.Entities.Users.Role
{
    /// <summary>
    /// Enum that represents the different types of roles that a user can have
    /// </summary>
    public enum RoleType
    {
        ADMIN,
        ENDUSER,
        CAMPUS_MANAGER,
        TASK_MANAGER,
        FLEET_MANAGER,
    }
}